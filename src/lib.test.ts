import { describe, expect, it } from 'vitest';
import { calculatePrizes, contributionAmount, generateNumbers, saveScore, validateScore } from './domain';

describe('score rules', () => { it('accepts a valid score', () => expect(validateScore(32, '2026-09-20', [])).toBeNull()); it('rejects invalid range and duplicate date', () => { expect(validateScore(46, '2026-09-20', [])).toContain('1 to 45'); expect(validateScore(20, '2026-09-20', [{ id:'1', date:'2026-09-20', value:20 }])).toContain('already'); }); it('keeps five newest scores', () => { const scores = Array.from({length: 5}, (_, i) => ({id:String(i),date:`2026-09-${10+i}`,value:20})); expect(saveScore(scores,{id:'new',date:'2026-09-20',value:35})).toHaveLength(5); expect(saveScore(scores,{id:'new',date:'2026-09-20',value:35})[0].id).toBe('new'); }); });
describe('charity and draws', () => { it('calculates minimum and increased contribution', () => { expect(contributionAmount('monthly', 10)).toBe(1.2); expect(contributionAmount('yearly', 25)).toBe(30); }); it('generates unique numbers', () => { expect(new Set(generateNumbers('random', [], () => .2)).size).toBe(5); }); it('splits tiers and rolls jackpot', () => { const prizes = calculatePrizes(1000,[{tier:3,count:2},{tier:4,count:1},{tier:5,count:0}],500); expect(prizes.find(p=>p.tier===5)?.total).toBe(900); expect(prizes.find(p=>p.tier===3)?.each).toBe(125); }); });
